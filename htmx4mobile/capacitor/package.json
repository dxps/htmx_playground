{
  "name": "htmx4mobile-capacitor",
  "version": "4.0.0",
  "private": true,
  "description": "Capacitor-packaged HTMX movie CRUD app with IndexedDB storage.",
  "scripts": {
    "dev": "npx http-server public -a 127.0.0.1 -p 4273 -c-1",
    "test": "playwright test",
    "cap:add:android": "npx cap add android",
    "cap:add:ios": "npx cap add ios",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android",
    "cap:open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@playwright/test": "^1.54.0",
    "http-server": "^14.1.1"
  }
}
