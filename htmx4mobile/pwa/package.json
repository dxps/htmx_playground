{
  "name": "htmx4mobile",
  "version": "4.0.0",
  "private": true,
  "description": "Installable HTMX PWA movie CRUD with offline caching.",
  "scripts": {
    "dev": "npx http-server public -a 127.0.0.1 -p 4173 -c-1",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "http-server": "^14.1.1"
  }
}
