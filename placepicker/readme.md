# Place Picker

A simple project that uses HTMX to select a place from a list of places.

<br/>

## Start

First, run `npm install` to install the Node.js dependencies.

Then, run `npm start` (or use `./dev.sh` provided script) to start the back-end.

And the UI is available at `http://localhost:3011`.
